# Copy to .env.local and fill in. .env.local is gitignored.

# Signs the session cookie. Must be at least 32 characters.
# Generate one with: openssl rand -base64 32
AUTH_SECRET=

# The single shared password that unlocks /dashboard.
DASHBOARD_PASSWORD=
